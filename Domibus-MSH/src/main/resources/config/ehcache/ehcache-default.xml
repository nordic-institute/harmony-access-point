<?xml version="1.0" encoding="UTF-8"?>
<config
        xmlns:xsi='http://www.w3.org/2001/XMLSchema-instance'
        xmlns:jsr107='http://www.ehcache.org/v3/jsr107'
        xmlns='http://www.ehcache.org/v3'
        xsi:schemaLocation="
        http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.5.xsd
        http://www.ehcache.org/v3/jsr107 http://www.ehcache.org/schema/ehcache-107-ext-3.5.xsd">

    <service>
        <jsr107:defaults enable-management="false" enable-statistics="true"/>
    </service>

    <!-- cache definitions start here -->
    <cache alias="policyCache" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="backendFilterCache" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="tlsCache" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="certValidationByAlias" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="crlByCert" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="lookupInfo" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="dispatchClient" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="auditTarget" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="userDomain" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="preferredUserDomain" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="allDomains" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="domainByCode" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="domainByScheduler" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="dss-cache">
        <expiry>
            <ttl>600</ttl>
        </expiry>
        <heap unit="MB">5</heap>
    </cache>

    <cache alias="encryptionKey" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="passwordEncryptionKey" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="multitenantCache" uses-template="ttl-3600-heap-5mb"/>

    <cache alias="jmsDestinations">
        <expiry>
            <ttl>300</ttl>
        </expiry>
        <heap unit="MB">5</heap>
    </cache>

    <cache alias="domibusProperties" uses-template="ttl-3600-heap-5mb"/>

    <!-- cache definitions ends here -->

    <cache-template name="ttl-3600-heap-5mb">
        <expiry>
            <ttl>3600</ttl>
        </expiry>
        <heap unit="MB">5</heap>
    </cache-template>
</config>